const config = {
    appPort: 3030,
    HOST: 'localhost',
    DATABASE: 'delilah_resto',
    USER_DATABASE: 'root',
    PASSWORD_DATABASE: 'querty123',
    apiVersion: "/v1",
    SECRET: 'DELILAH-RESTO'
}

module.exports = config;