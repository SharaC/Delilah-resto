const config = {
    appPort: 3030,
    HOST_DATABASE: 'localhost',
    NAME_DATABASE: 'delilah_resto',
    USER_DATABASE: 'usuario_database',
    PASSWORD_DATABASE: 'password_usuario',
    apiVersion: "/v1",
    SECRET: 'DELILAH-RESTO'
}

module.exports = config;
